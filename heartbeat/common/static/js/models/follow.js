define([
    'jquery',
    'backbone',
    'vent',
], function($, Backbone, vent) {

  var Follow = Backbone.Model.extend({
    defaults: {
      artist_id: -1,
      user_id: -1,
      autoGenerated: false,
    },
    initialize: function() {
      vent.bind("follow", this.savefollow);
    },

  });
  return Follow;
});
